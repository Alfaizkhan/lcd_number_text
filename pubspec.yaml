name: lcd_number_text
description: An automatic sizer digital single-digit number with LCD style widget.
version: 0.0.1
# homepage:
repository: https://github.com/Alfaizkhan/lcd_number_text

environment:
  sdk: '>=3.0.5 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:

